[package]
name = "tupa-validator"
version = "0.8.0"
edition = "2021"
license = "MIT"
description = "Compatibility facade for Tupã program validation"

[dependencies]
tupa-parser = { path = "../tupa-parser" }
tupa-typecheck = { path = "../tupa-typecheck" }